import {Router} from "express";import {pool} from "../db.js";import {requireAuth,requireAdmin} from "../middleware/auth.js";const router=Router();router.get("/",requireAuth,requireAdmin,async(req,res)=>{const r=await pool.query(`SELECT a.*,u.name user_name,w.name warehouse_name FROM audit_logs a LEFT JOIN users u ON u.id=a.user_id LEFT JOIN warehouses w ON w.id=a.warehouse_id ORDER BY a.created_at DESC LIMIT 500`);res.json(r.rows)});export default router;
