import{useState}from"react";import{useNavigate}from"react-router-dom";import{api}from"../api";export default function Login(){const[email,setEmail]=useState("admin@demo.com"),[password,setPassword]=useState("admin123"),[msg,setMsg]=useState(""),nav=useNavigate();async function submit(e){e.preventDefault();try{const r=await api("/auth/login",{method:"POST",body:JSON.stringify({email,password})});localStorage.setItem("token",r.token);localStorage.setItem("user",JSON.stringify(r.user));nav("/")}catch(e){setMsg(e.message)}}return <div className="login"><form onSubmit={submit}><h1>StockPilot</h1><p>Inventory Management System</p><label>Email<input value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="primary">Sign in</button>{msg&&<p className="error">{msg}</p>}</form></div>}
