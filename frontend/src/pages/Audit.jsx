import{useEffect,useState}from"react";import{api}from"../api";export default function Audit(){const[a,setA]=useState([]);useEffect(()=>{api("/audit").then(setA)},[]);return <><header><p>Security and traceability</p><h1>Audit Logs</h1></header><div className="table">{a.map(x=><div className="trow" key={x.id}><span>{x.action}<small>{x.entity_type||"-"}</small></span><span>{x.user_name||"System"}</span><span>{x.warehouse_name||"-"}</span><span>{new Date(x.created_at).toLocaleString()}</span></div>)}</div></>}
