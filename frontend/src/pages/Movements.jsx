import{useEffect,useState}from"react";import{api}from"../api";export default function Movements(){const[m,setM]=useState([]);useEffect(()=>{api("/dashboard/movements").then(setM)},[]);return <><header><p>Inventory history</p><h1>Stock Movements</h1></header><div className="table">{m.map(x=><div className="trow" key={x.id}><span>{x.product_name}<small>{x.sku}</small></span><span>{x.movement_type}</span><span>{x.quantity}</span><span>{x.user_name||"System"}</span><span>{new Date(x.created_at).toLocaleString()}</span></div>)}</div></>}
